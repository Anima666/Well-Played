using UnityEngine;
using Assets.Scripts;
using UnityEngine.UI;

public class SetLanguage : MonoBehaviour {

    // Use this for initialization
    void Start()
    {
      //  Debug.Log("LOl kek " + Globalper.curr_lang);
       // GameObject[] gui = GameObject.FindGameObjectsWithTag("gui");
        //if (Global2.curr_lang==Country.en)
        //{
        //    for (int i = 0; i < gui.Length; i++)
        //    {
        //        gui[i].GetComponentInChildren<Text>().text = gui[i].name;
        //    }
          
        //}
    }
        // Update is called once per frame
        void Update () {
	
	}
}
